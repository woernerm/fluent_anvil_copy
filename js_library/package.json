{
  "name": "fluent_anvil_js",
  "version": "1.1.0",
  "description": "JavaScript interface to Project Fluent for Anvil.",
  "source": "src/fluent_anvil.js",
  "module": "dist/fluent_anvil.js",
  "browserslist": "> 5%, last 2 versions, not dead",
  "targets": {
    "module": {
      "context": "browser",
      "outputFormat": "esmodule",
      "optimize": true,
      "includeNodeModules": true
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel build --detailed-report --log-level verbose"
  },
  "author": "Marcus Wörner",
  "license": "MIT",
  "devDependencies": {
    "@parcel/compressor-brotli": "latest",
    "@parcel/compressor-gzip": "latest",
    "@parcel/optimizer-esbuild": "latest",
    "@parcel/optimizer-terser": "latest",
    "@swc/cli": "latest", 
    "@swc/core": "latest",
    "parcel": "latest"
  },
  "dependencies": {
    "@fluent/bundle": "^0.18.0",
    "@fluent/dom": "^0.9.0",
    "@formatjs/intl-localematcher": "^0.5.0",
    "get-user-locale": "^2.3.1",
    "intl-pluralrules": "^2.0.1"
  }
}
